{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Local (offline) MCP servers for the wpf-net472-airgap-dev-pack plugin. Executables are vendored under bin/ by tools/build-local-bin.ps1 — there is NO dnx / NuGet resolution at process start, so the servers keep starting after global NuGet/uv cache cleanup. Run tools/build-local-bin.ps1 once during bundle prep.",
  "mcpServers": {
    "WpfDevPackMcp": {
      "type": "stdio",
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/WpfDevPackMcp/WpfDevPackMcp.exe",
      "env": {
        "WPFDEVPACK_OFFLINE": "1"
      }
    },
    "HandMirrorMcp": {
      "type": "stdio",
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/HandMirrorMcp/handmirror.exe"
    }
  }
}
